// App configuration
export const API_CONFIG = {
  baseURL: __DEV__ ? 'http://localhost:3000' : 'https://api.production.com',
  timeout: 10000,
};

export const APP_CONFIG = {
  name: 'Quests',
  version: '1.0.0',
};

